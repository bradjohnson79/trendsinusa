# Prisma expects these env vars for the datasource in `prisma/schema.prisma`.
# Copy to `packages/db/.env` (do not commit secrets).

# pooled Neon URL:
DATABASE_URL=

# direct Neon URL:
DATABASE_DIRECT_URL=

