Deterministic placeholder source SVGs.

These SVGs are **not** used directly by the frontend. They are converted to PNG via Sharp
and written to `apps/web/public/placeholders/*.png`.

Run:
- `pnpm placeholders:build`

